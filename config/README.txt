ローカル専用。Git にコミットしない。

  service-account.json  … seed スクリプト用（Firebase Console から DL したキー）
  extra_ca.pem            … 社内プロキシ用 CA（必要な場合のみ）

FireMint アプリ本体はファイル選択ダイアログでどこからでも読める。
キーをここに置くのは seed 等の CLI 用。
